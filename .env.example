# cp .env.example .env && source .env
export DATABRICKS_PROFILE=dbx-test
export DATABRICKS_WAREHOUSE_ID=228702d3505041c1
export DEMO_CATALOG=workspace
export DEMO_SCHEMA=governance_demo
export GOV_SCHEMA=governance

# is_account_group_member is right for account groups. On a trial workspace
# whose groups are workspace-local, is_member is the one that resolves.
# Run `make whoami` to see which returns true for you.
export MEMBER_FN=is_account_group_member
